export function MarketlyFooter() {
  return (
    <footer className="mt-auto border-t border-zinc-200 bg-white py-6 text-center text-xs text-zinc-500">
      © {new Date().getFullYear()} Marketly — demo catalog (Fake Store API).
    </footer>
  );
}
